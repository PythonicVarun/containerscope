name: Publish Container Images

on:
    push:
        tags:
            - "v*.*.*"
    workflow_dispatch:

env:
    GHCR_REGISTRY: ghcr.io
    DOCKERHUB_REGISTRY: docker.io
    IMAGE_NAME: containerscope
    DOCKERHUB_NAMESPACE: ${{ vars.DOCKERHUB_NAMESPACE }}

jobs:
    publish:
        runs-on: ubuntu-latest
        permissions:
            contents: read
            packages: write

        steps:
            - name: Check out repository
              uses: actions/checkout@v6

            - name: Set up QEMU
              uses: docker/setup-qemu-action@v4

            - name: Set up Docker Buildx
              uses: docker/setup-buildx-action@v4

            - name: Log in to GHCR
              uses: docker/login-action@v4
              with:
                  registry: ${{ env.GHCR_REGISTRY }}
                  username: ${{ github.actor }}
                  password: ${{ secrets.GITHUB_TOKEN }}

            - name: Log in to Docker Hub
              uses: docker/login-action@v4
              with:
                  registry: ${{ env.DOCKERHUB_REGISTRY }}
                  username: ${{ secrets.DOCKERHUB_USERNAME || env.DOCKERHUB_NAMESPACE }}
                  password: ${{ secrets.DOCKERHUB_TOKEN }}

            - name: Capture build timestamp
              id: vars
              run: echo "build_date=$(date -u +%Y-%m-%dT%H:%M:%SZ)" >> "$GITHUB_OUTPUT"

            - name: Extract image metadata
              id: meta
              uses: docker/metadata-action@v6
              with:
                  images: |
                      ${{ env.GHCR_REGISTRY }}/${{ github.repository_owner }}/${{ env.IMAGE_NAME }}
                      ${{ env.DOCKERHUB_REGISTRY }}/${{ env.DOCKERHUB_NAMESPACE }}/${{ env.IMAGE_NAME }}
                  labels: |
                      org.opencontainers.image.title=ContainerScope
                      org.opencontainers.image.description=Live Docker container log viewer for Docker hosts
                  tags: |
                      type=raw,value=latest,enable={{is_default_branch}}
                      type=ref,event=tag
                      type=sha,prefix=sha-

            - name: Build and push image
              uses: docker/build-push-action@v7
              with:
                  context: .
                  platforms: linux/amd64,linux/arm64
                  push: true
                  labels: ${{ steps.meta.outputs.labels }}
                  tags: ${{ steps.meta.outputs.tags }}
                  build-args: |
                      VERSION=${{ steps.meta.outputs.version }}
                      VCS_REF=${{ github.sha }}
                      BUILD_DATE=${{ steps.vars.outputs.build_date }}
